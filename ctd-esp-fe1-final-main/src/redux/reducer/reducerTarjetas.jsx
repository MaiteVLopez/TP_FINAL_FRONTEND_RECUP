const estadoInicial = {
    tarjetas: []
};

export const estadoFiltro = (strate = estadoInicial, action) => {
    switch (action.type){
        case "": 
            break;
        default:
            break;
    }
} 
